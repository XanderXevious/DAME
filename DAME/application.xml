<?xml version="1.0" encoding="utf-8" ?> 
<application xmlns="http://ns.adobe.com/air/application/2.0">


	
	
	
	<!-- 
	More options:
	http://livedocs.adobe.com/flex/3/html/File_formats_1.html#1043413
	-->




<!-- Adobe AIR Application Descriptor File Template.

	Specifies parameters for identifying, installing, and launching AIR applications.

	xmlns - The Adobe AIR namespace: http://ns.adobe.com/air/application/1.5
			The last segment of the namespace specifies the version 
			of the AIR runtime required for this application to run.
			
	minimumPatchLevel - The minimum patch level of the AIR runtime required to run 
			the application. Optional.
-->

	<!-- The application identifier string, unique to this application. Required. -->
	<id>DAME.dambots</id>

	<!-- Used as the filename for the application. Required. -->
	<filename>DAME</filename>

	<!-- The name that is displayed in the AIR application installer. 
	     May have multiple values for each language. See samples or xsd schema file. Optional. -->
	<name>DAME</name>

	<!-- An application version designator (such as "v1", "2.5", or "Alpha 1"). Required. -->
	<version>3.1.4</version>

	<!-- Description, displayed in the AIR application installer.
	     May have multiple values for each language. See samples or xsd schema file. Optional. -->
	<description>DAME - Deadly Alien Map Editor for 2D games.</description>

	<!-- Copyright information. Optional -->
	<copyright>Copyright 2010-2013 - Charles Goatley</copyright>

	<!-- Settings for the application's initial window. Required. -->
	<initialWindow>
		<!-- The main SWF or HTML file of the application. Required. -->
		<!-- Note: In Flex Builder, the SWF reference is set automatically. -->
		<content>DAME.swf</content>
		
		<!-- The title of the main window. Optional. -->
		<title>DAME</title>

		<!-- The type of system chrome to use (either "standard" or "none"). Optional. Default standard. -->
		<systemChrome>standard</systemChrome>

		<!-- Whether the window is transparent. Only applicable when systemChrome is none. Optional. Default false. -->
		<transparent>false</transparent>

		<!-- Whether the window is initially visible. Optional. Default false. -->
		<visible>true</visible>

		<!-- Whether the user can minimize the window. Optional. Default true. -->
		<minimizable>true</minimizable>

		<!-- Whether the user can maximize the window. Optional. Default true. -->
		<maximizable>true</maximizable>

		<!-- Whether the user can resize the window. Optional. Default true. -->
		<resizable>true</resizable>

		<!-- The window's initial width. Optional. -->
		<width>900</width>

		<!-- The window's initial height. Optional. -->
		<height>700</height>

		<!-- The window's initial x position. Optional. -->
		<!-- <x></x> -->

		<!-- The window's initial y position. Optional. -->
		<!-- <y></y> -->

		<!-- The window's minimum size, specified as a width/height pair, such as "400 200". Optional. -->
		<minSize>640 480</minSize>

		<!-- The window's initial maximum size, specified as a width/height pair, such as "1600 1200". Optional. -->
		<!-- <maxSize></maxSize> -->
	</initialWindow>

	<!-- The subpath of the standard default installation location to use. Optional. -->
	<!-- <installFolder></installFolder> -->

	<!-- The subpath of the Programs menu to use. (Ignored on operating systems without a Programs menu.) Optional. -->
	<programMenuFolder>DAME</programMenuFolder>

	<!-- The icon the system uses for the application. For at least one resolution,
		 specify the path to a PNG file included in the AIR package. Optional. -->
	<icon>
		<image16x16>AppIcon16.png</image16x16>
		<image32x32>AppIcon32.png</image32x32>
		<image48x48>AppIcon48.png</image48x48>
		<image128x128>AppIcon128.png</image128x128>
	</icon>
	

	<!-- Whether the application handles the update when a user double-clicks an update version
	of the AIR file (true), or the default AIR application installer handles the update (false).
	Optional. Default false. -->
	<!-- <customUpdateUI></customUpdateUI> -->
	
	<!-- Whether the application can be launched when the user clicks a link in a web browser.
	Optional. Default false. -->
	<!-- <allowBrowserInvocation></allowBrowserInvocation> -->

	<!-- Listing of file types for which the application can register. Optional. -->
	<fileTypes>

		<!-- Defines one file type. Optional. -->
		<fileType>

			<!-- The name that the system displays for the registered file type. Required. -->
			<name>DAME.mapfile</name>

			<!-- The extension to register. Required. -->
			<extension>dam</extension>
			
			<!-- The description of the file type. Optional. -->
			<description>DAME map file</description>
			
			<!-- The MIME content type. -->
			<contentType>text/plain</contentType>
			
			<!-- The icon to display for the file type. Optional. -->
			<icon>
				<image16x16>AppIcon16.png</image16x16>
				<image32x32>AppIcon32.png</image32x32>
				<image48x48>AppIcon48.png</image48x48>
				<image128x128>AppIcon128.png</image128x128>
			</icon>
			
		</fileType>
	</fileTypes>

</application>
